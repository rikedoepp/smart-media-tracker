# Copy to .streamlit/secrets.toml and fill in
[bigquery]
dataset = "your_dataset"
table = "your_table"
# Optional if your project is multi-region
location = "EU"

# Authentication:
# Use Application Default Credentials, e.g. set env GOOGLE_APPLICATION_CREDENTIALS
# or deploy on a platform with service account attached.
